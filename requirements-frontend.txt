# Optional manual frontends only — not required for the library or CI.
# gradio_client (Gradio API testing) ships with gradio; streamlit.testing.v1
# (AppTest) ships with streamlit — no extra browser/JS tooling needed for those.
gradio>=4.0
streamlit>=1.28
playwright>=1.40  # only for tests/frontend/test_chat_ui_screen.py; run `playwright install chromium` once
