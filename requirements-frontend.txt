# Optional manual frontends only — not required for the library or CI.
# gradio_client (Gradio API testing) ships with gradio; streamlit.testing.v1
# (AppTest) ships with streamlit — no extra browser/JS tooling needed.
gradio>=4.0
streamlit>=1.28
